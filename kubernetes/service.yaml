apiVersion: v1
kind: Service
metadata:
  name: node-app-service
  namespace: production-app
  labels:
    app: node-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: node-app
